---
title: "phase_angle_diff_fig"
author: "MOEKA"
date: "2024-11-15"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

library(lubridate)
library(dplyr)
library(ggplot2)
library(ggpubr)
library(tidyr)
library(gridExtra)
library(purrr)
library(patchwork)
```



```{r}
filenames_raw <- 
  list.files("G:/My Drive/Research/Projects/DC_auto/Data/Result/GPP_SR_cospectral/raw_64d_one_GPP/df_diurnal/", "*.csv", full.names = T)

filenames_res <- 
  list.files("G:/My Drive/Research/Projects/DC_auto/Data/Nov2024_rerun/Re_result_pad_64d_res/df_diurnal/", "*.csv", full.names = T)


ts_raw_files <- grep("_Ts\\.csv", filenames_raw, value = TRUE)
#par_res_files <- grep("_PAR\\.csv", filenames_res, value = TRUE)
par_raw_files <- grep("_PAR\\.csv", filenames_raw, value = TRUE)
gpp_raw_files <- grep("_GPP\\.csv", filenames_raw, value = TRUE)
```



```{r}
phase_diff_csv <- function(filename){
  
  df <- strsplit(filename, "/")[[1]][length(strsplit(filename, "/")[[1]])]
  campaign <- strsplit(df, "_")[[1]][3]
  resp <- strsplit(df, "_")[[1]][2]
  driver <- sub(".csv","", strsplit(df, "_")[[1]][4])
  
  result <- 
    read.csv(filename) %>%
    select(c(date, NA_flag, pval_period1, Angle_raw)) %>% 
    mutate(day = as.Date(date), 
         Angle_raw_2 = ifelse(pval_period1 <= .1, Angle_raw, NA)) %>% 
    group_by(day) %>% 
    summarise(angle_raw_avg = mean(Angle_raw)) %>% 
    mutate(phase_hr = (angle_raw_avg * 24) / (2 * pi))
  
  result %>% 
    summarise(
      Phase_hr_avg = mean(phase_hr, na.rm = T),
      Phase_hr_sd = sd(phase_hr, na.rm = T)
      ) %>% 
    mutate(
      df = df,
      campaign = campaign,
      resp = resp,
      driver = driver
    ) 
}


ts_phase <- purrr::map_dfr(ts_raw_files, phase_diff_csv)

campaign <- c("MJ22" = "C1", "MA23" = "C2", "Mar24" = "C3", 
              "Apr24" = "C4", "Sep24" = "C5", "Oct24" = "C6")

ts_phase <- 
  ts_phase %>%
  mutate(campaign_num = recode(campaign, !!!campaign))


#par_res_phase <- purrr::map_dfr(par_res_files, phase_diff_csv)


# # csv
# write.csv(par_res_phase_cam, "G:/My Drive/Research/Projects/DC_auto/Data/Nov2024_rerun/Re_PAR_res_phase.csv", row.names = F)

par_raw_phase <- purrr::map_dfr(par_raw_files, phase_diff_csv)

par_raw_phase <- 
  par_raw_phase %>%
  mutate(campaign_num = recode(campaign, !!!campaign))

gpp_raw_phase <- purrr::map_dfr(gpp_raw_files, phase_diff_csv)

gpp_raw_phase <- 
  gpp_raw_phase %>%
  mutate(campaign_num = recode(campaign, !!!campaign))


# combined and save
phase_raw_combined <- 
  rbind(ts_phase, par_raw_phase, gpp_raw_phase)

write.csv(phase_raw_combined, "G:/My Drive/Research/Projects/DC_auto/Data/Result/GPP_SR_cospectral/raw_64d_one_GPP/df_lag_hr/phase_raw_combined.csv", row.names = F)
```


```{r graph}

phase_diff_graph <- function(df, res, driver){
  
  df %>% 
  filter(resp == res) %>% 
  ggplot(aes(campaign_num, Phase_hr_avg)) +
  geom_point(size = 5) + 
  geom_errorbar(
    aes(campaign_num,
        ymin = Phase_hr_avg - Phase_hr_sd,
        ymax = Phase_hr_avg + Phase_hr_sd,
        ),
    lwd = 1.5, width = .2
  ) +
  geom_abline(slope = 0, intercept = 0) + 
  theme_classic() + 
  ylim(-8, 8) +
  ggtitle(paste(res, "vs", driver)) +
  ylab("Lag (Hour)") + 
  theme(
    axis.title.x = element_blank(),
    axis.text.x = element_text(size = 15),
    axis.title.y = element_text(size = 15),
    axis.text.y = element_text(size = 15),
    axis.ticks.x = element_blank(),
    panel.border = element_rect(colour = "black", fill = NA, linewidth = 1)
        )
}



p_Rh_PAR <- phase_diff_graph(par_raw_phase, "Rh", "PAR")
p_Ra_PAR <- phase_diff_graph(par_raw_phase, "Ra", "PAR")

p_Rh_Ts <- phase_diff_graph(ts_phase, "Rh", "Ts")
p_Ra_Ts <- phase_diff_graph(ts_phase, "Ra", "Ts")

p_Rh_GPP <- phase_diff_graph(gpp_raw_phase, "Rh", "GPP")
p_Ra_GPP <- phase_diff_graph(gpp_raw_phase, "Ra", "GPP")


combined_plot <- (p_Rh_GPP + p_Rh_PAR + p_Rh_Ts) / (p_Ra_GPP + p_Ra_PAR + p_Ra_Ts) & ylab(NULL)
wrap_elements(combined_plot) +
  labs(tag = "Lag (hour)") +
  theme(
    plot.tag = element_text(size = 15, angle = 90),
    plot.tag.position = "left"
  )

# res
phase_diff_graph(par_res_phase_cam, "rRh", "PAR")
phase_diff_graph(par_res_phase_cam, "rRa", "PAR")

```